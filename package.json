{
  "name": "campaign-manager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-lambda": "aws-lambda-upload --zip /builds "
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "aws-lambda-upload": "^0.2.3",
    "aws-sdk": "^2.626.0"
  },
  "dependencies": {
    "mailgun-js": "^0.22.0",
    "supports-color": "^7.1.0",
    "zip": "^1.2.0"
  }
}
